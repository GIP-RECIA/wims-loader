:{{ structure.parent }}/{{ structure.id_class }},{{ structure.expiration|wims_date }},{{ structure.institution_name }},{{ structure.description }},{{ structure.lang }},{{ structure.type }},{{ structure.level }},{{ structure.password|pass_crypt[:3] }},{{ supervisor.first_name }} {{ supervisor.last_name }},
